# [optional] to synchronize system time of nodes with 'chrony'
- hosts:
  - test
  remote_user: root
  gather_facts: true
  ignore_errors: no
  vars:
    mysql_port: 3306
  roles:
    - {role: 'mysql'}
