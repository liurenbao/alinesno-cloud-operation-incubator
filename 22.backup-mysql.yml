# [optional] to synchronize system time of nodes with 'chrony'
- hosts:
  - all
  remote_user: root
  gather_facts: true
  ignore_errors: no
  vars:
    db_user: 'root'
    # db_passwd: 'HouGuiYu@123!@#'
    db_passwd: 'LuoAnDong@Switch'
    backup_dir: '/db/backup'
  roles:
  - { role: 'backup-mysql' }

