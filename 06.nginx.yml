# [optional] to synchronize system time of nodes with 'chrony'
- hosts:
  - nginx
  remote_user: root
  gather_facts: true
  ignore_errors: no
  vars:
    - server_hostname: 127.0.0.1
  roles:
  - { role: 'nginx'}
