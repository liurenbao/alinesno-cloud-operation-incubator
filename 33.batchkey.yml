# [optional] to synchronize system time of nodes with 'chrony'
- hosts:
  - all
  remote_user: root
  gather_facts: true
  become: yes
  become_user: root
  become_method: su 
  ignore_errors: no 
  roles:
    - { role : 'batchkey' }
