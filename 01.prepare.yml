# [optional] to synchronize system time of nodes with 'chrony'
- hosts:
  - test
  remote_user: root
  gather_facts: true
  ignore_errors: no
  vars:
    new_user_name : 'alinesno'
  roles:
  - { role: 'chrony', when: "groups['chrony']|length > 0" }
  - { role: 'prepare' }
