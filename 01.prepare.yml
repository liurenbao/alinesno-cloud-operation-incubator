# [optional] to synchronize system time of nodes with 'chrony'
- hosts:
  - all
  remote_user: root
  gather_facts: true
  ignore_errors: no
  vars:
    new_user_name : 'alinesno'
  roles:
  - { role: 'chrony' }
  - { role: 'prepare' }
